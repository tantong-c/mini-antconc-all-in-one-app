# 🐜 Mini-AntConc Online (All-in-One Version)

**Mini-AntConc Online** คือเว็บแอปพลิเคชันสำหรับวิเคราะห์คลังข้อมูลภาษา (Corpus Analysis Tool) ที่พัฒนาด้วย Python และ Streamlit 

เวอร์ชันนี้เป็นแบบ **All-in-One** ที่รวบรวมความสามารถในการจัดการไฟล์ไว้ในที่เดียว ไม่ว่าคุณจะมีไฟล์ดิบ (Raw Text) หรือไฟล์ที่ตัดคำมาแล้ว โปรแกรมนี้ก็พร้อมทำงานทันทีด้วยระบบตัดคำอัตโนมัติจาก **PyThaiNLP**

## ✨ ฟีเจอร์หลัก (Key Features)

### 1. 🛠️ ระบบจัดการข้อมูลที่ยืดหยุ่น (Data Processing)
* **Auto Tokenization:** รองรับการอัปโหลดไฟล์ข้อความทั่วไป โดยโปรแกรมจะตัดคำภาษาไทยให้โดยอัตโนมัติ (ใช้ engine `newmm` ของ PyThaiNLP)
* **Manual Mode:** รองรับไฟล์ที่ตัดคำมาแล้ว (คั่นด้วย `|`) สำหรับงานที่ต้องการความแม่นยำเฉพาะทาง

### 2. 🔍 Concordance Tool (KWIC) ขั้นสูง
* ค้นหาคำศัพท์และแสดงบริบท (Keyword In Context)
* **Advanced Search Syntax:** รองรับการค้นหาที่ซับซ้อน (Wildcards, Gaps)
* แสดงชื่อไฟล์ต้นทางในตารางผลลัพธ์
* ดาวน์โหลดผลลัพธ์เป็นไฟล์ CSV

### 3. 📊 เครื่องมือวิเคราะห์อื่นๆ
* **Word List:** นับความถี่คำและจัดลำดับ (Frequency Rank)
* **N-Grams:** วิเคราะห์กลุ่มคำที่ปรากฏร่วมกัน (Clusters) ตามขนาด N ที่กำหนด
* **File Viewer:** ดูเนื้อหาไฟล์ต้นฉบับพร้อมแสดงจุดที่ถูกตัดคำ (`|`)

### 4. 🎨 User Interface
* **Soft Light Theme:** ออกแบบสีถนอมสายตา อ่านง่าย
* **Interactive Tables:** ตารางเรียงลำดับข้อมูลได้ และเริ่มนับลำดับที่ 1 เสมอ

---

## 📖 คู่มือการใช้คำค้นหา (Search Syntax)

ในหน้า Concordance คุณสามารถใช้รูปแบบการค้นหาได้ 7 รูปแบบ ดังนี้:

| รูปแบบ | ความหมาย | ตัวอย่าง | ผลลัพธ์ที่ได้ |
| :--- | :--- | :--- | :--- |
| **คำ** | ค้นหาคำนั้นๆ (Exact Match) | `รัก` | เจอคำว่า "รัก" เท่านั้น |
| **\*คำ** | ลงท้ายด้วยคำนี้ (Ends with) | `*รัก` | น่ารัก, ความรัก |
| **คำ\*** | ขึ้นต้นด้วยคำนี้ (Starts with) | `รัก*` | รักษา, รักใคร่ |
| **\*คำ\*** | มีคำนี้ผสมอยู่ (Contains) | `*รัก*` | อนุรักษ์, องครักษ์ |
| **คำ1 คำ2** | คำ 2 คำติดกัน | `รัก มาก` | รักมาก |
| **คำ1 <N> คำ2** | ห่างกัน N คำพอดี | `รัก <3> มาก` | รัก (x) (x) (x) มาก |
| **คำ1 <N-M> คำ2** | ห่างกันตั้งแต่ N ถึง M คำ | `รัก <1-3> มาก` | เจอทั้งห่าง 1, 2, หรือ 3 คำ |

---

## 💻 การติดตั้งและใช้งาน (Installation)

### 1. สิ่งที่ต้องเตรียม (Prerequisites)
* Python 3.8 หรือใหม่กว่า
* Git (สำหรับการ Clone project)

### 2. ติดตั้ง Library
สร้างไฟล์ `requirements.txt` และใส่รายชื่อไลบรารีดังนี้:
```text
streamlit
pandas
pythainlp